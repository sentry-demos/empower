#!/bin/bash

# Git post-checkout hook to merge upstream changes from .local.env.base into local.env
# This script runs after git checkout, git switch, and git pull --rebase operations

set -e

# Use the new git-based merge system
REPO_ROOT=$(git rev-parse --show-toplevel)
"$REPO_ROOT/_bin/merge-local-env"

exit 0