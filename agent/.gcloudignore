# Override root .gitignore for Cloud Build
# We need .env to be included (generated from _.env.template)

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/

# IDE
.idea/
.vscode/
*.swp

# Local development only
docker-compose.yml
run_local.sh

# Do NOT ignore .env - it's needed for Cloud Run
# (it's generated from _.env.template by the deploy script)
