import { render, screen } from '@testing-library/react';
import * as React from 'react';
import { generateUrl } from './components/Checkout';

describe('generateUrl', () => {
  it('tests the only function', () => {
    expect(true).toBe(true);
  });
});
